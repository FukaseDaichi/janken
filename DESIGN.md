# DESIGN.md — デザインルール

「じゃんけんサバイバー」の見た目に関する取り決めをゆるくまとめたもの。
厳密な仕様書ではなく、**新しく描画コードを足すときに迷わないための指針**。

コンセプトは **ネオンアーケード**。暗い紫紺の空間にネオンが発光している、
80〜90年代アーケード筐体風のビジュアル。

---

## 1. 定数の置き場所

**色・フォント・画面サイズの定数は `src/render/theme.ts` に一本化する。**
シーンやエンティティにハードコードしない（既存の逸脱は §8 参照）。

| 定数 | 値 | 意味 |
|---|---|---|
| `CANVAS_W` / `CANVAS_H` | 1200 / 720 | canvas 内部解像度（5:3） |
| `PANEL_X` / `PANEL_W` | 960 / 240 | 右サイドパネルの位置と幅 |
| `FIELD_W` / `FIELD_H` | 960 / 720 | プレイフィールド（`entities/player.ts`） |

`index.html` の `<canvas width height>` 属性は初期値にすぎない。
CSS 側は `aspect-ratio: 5 / 3` と `min()` でウィンドウに合わせて拡縮する（内部解像度は固定）。

---

## 2. カラーパレット

### ベース

| 用途 | 色 |
|---|---|
| 背景（深部） | `#0a0618` — `COLORS.bgDeep` |
| ページ地（body） | `#05030e` |
| パネル地 | `rgba(10, 8, 30, 0.92)` — `COLORS.panelBg` |
| パネル枠 | `#5a3fd0` — `COLORS.panelBorder` |

### アクセント（意味を固定する）

| 色 | 値 | 割り当てられた意味 |
|---|---|---|
| シアン | `#37e0e8` | スコア・プロンプト（「進める」情報） |
| イエロー | `#ffd23e` | 倍率・LEVEL UP・勝利ピップ（「良いこと」） |
| レッド | `#ff3b4f` | GAME OVER・危険 |
| ホワイト | `#ffffff` | レベル数値など中立の強調 |

**アクセント色は意味に紐づいている。** 見た目の好みで別の意味に流用しない。

### 手の3すくみカラー（`HAND_COLORS`）

| 手 | base | glow |
|---|---|---|
| グー rock | `#5ad14f` | `#8dff70` |
| チョキ scissors | `#e8586f` | `#ff7d9c` |
| パー paper | `#3f9df0` | `#6fc4ff` |

`Record<Hand, ...>` で定義しているので、手を増やす場合は型エラーで漏れが検出される。
`base` は塗り、`glow` は発光・枠・テキストに使う（明度が高い方が glow）。

### 無敵アイテム（星）のカラー（`STAR_COLORS`）

| 用途 | 値 |
|---|---|
| core（中心のハイライト） | `#fff3a8` |
| base（星の本体） | `#ffd23e` |
| glow（外周グロー） | `#ffb03a` |

`base` は `COLORS.yellow` と同値。アイテムは「良いこと」なのでイエロー系に統一する。
弾（紫・オレンジ・シアン）とも手（緑・赤ピンク・青）とも色相が重ならない。

フラッシュ演出は `rgba()` のアルファを毎フレーム変えるため hex では使えない。
`FLASH_RGB`（`"R,G,B"` 形式）に `COLORS` と対応する値を持たせている。

### ラベル色

数値の見出し（SCORE / LV など）は白の低アルファで、値より一段引く。

- `COLORS.label` = `rgba(255,255,255,0.55)`
- `COLORS.labelStrong` = `rgba(255,255,255,0.7)`

---

## 3. タイポグラフィ

2書体だけ。混ぜない。

| 用途 | フォント | 定数 |
|---|---|---|
| 見出し・ロゴ・日本語 | **Dela Gothic One**（極太、袋文字向き） | `FONT_DISPLAY` |
| 数字・英字ラベル | **Orbitron**（等幅寄りのSF書体） | `FONT_NUM` |

- 読み込みは `index.html` の Google Fonts。`main.ts` で `document.fonts.ready` を待ってから
  ループを開始する（未ロードでの初回描画を避けるため）。
- 失敗時は `sans-serif` フォールバック。起動はブロックしない。
- 英字ラベルは **全部大文字 + Orbitron 700 / 15〜16px**（`SCORE` `MULTIPLIER` `LV.` `WINS` `YOU` `TARGET`）。
- 数値は `toLocaleString('en-US')` でカンマ区切り、レベルは `padStart(2, '0')` で2桁ゼロ埋め。

### 文字の描き方は2種類（`src/render/text.ts`）

- **`outlinedText()` — 袋文字**：黒縁 + 右下オフセットシャドウ。ロゴ・大見出し用。
  縁の太さの既定は `size * 0.18`、シャドウは `size * 0.08`。
- **`neonText()` — ネオングロー**：色付き `shadowBlur`（既定 `size * 0.5`）で発光させ、
  その上に `rgba(255,255,255,0.85)` を重ねて芯を白く抜く。HUD数値・点滅プロンプト用。

**この2つ以外の文字表現は増やさない。** 素の `fillText` はラベルなど小さい補助テキストに限る。

---

## 4. レイアウト

```
┌───────────────────────────────┬──────────┐
│                               │  SCORE   │
│      プレイフィールド            │ MULTI... │
│         960 × 720             │   LV.    │
│                               │  WINS    │
│                               │  YOU     │
│                               │ TARGET   │
└───────────────────────────────┴──────────┘
 x: 0                        960          1200
```

- **フィールド内にHUDテキストを置かない。** 数値情報はすべて右パネルへ。
  フィールドに重ねてよいのは演出（LEVEL UP! バナー、フラッシュ）だけ。
- **描画順は「フィールド → パネル」固定。** フィールド外にスポーンした敵・弾のはみ出しを
  パネルが覆う前提になっている。
- パネルは半透明なので、先に `bgDeep` の不透明下地を敷く
  （描画ループが canvas をクリアしないため、下地がないと残像が透ける）。
- パネル内は縦一列・中央揃え。`ラベル(小・低コントラスト) → 値(大・発光)` の対を縦に積む。

---

## 5. 背景（`src/render/background.ts`）

状態を持たない純粋な描画関数。アニメ位相は `timeSec` 引数で受ける。

2モードあり、**用途で使い分ける**：

- **`perspective: true`（タイトルのみ）** — 消失点のある遠近グリッド床 + 地平線グロー。
  奥行き演出。
- **`perspective: false`（既定 / プレイ・ゲームオーバー）** — 見下ろし型のフラットグリッド。
  `CELL = 60px`、4本ごとに明るい major 線。中央からのラジアルグローで「場」の中心を示す。

見下ろし画面で遠近グリッドを使わないのは、**プレイヤーが2D平面を自由移動する画面で
消失点のある床を敷くと「宙に浮いている」ように誤読されるため**。ここは意図的な使い分け。

共通の重ね順：

1. 縦グラデーションの空間ベース
2. グリッド（紫 `rgba(170,100,255,...)` 系）
3. 浮遊光点 24個 — 位置は決定的に散らし、明滅だけ `sin(timeSec)` で揺らす
4. ビネット（外周 `rgba(0,0,0,0.55)` まで落とす）

**ランダム配置は使わない。** 光点は疑似ランダムな決定的計算（`k * 379 % 997` など）で散らす。

---

## 6. エンティティの発光ルール

すべての発光は **加算合成 `globalCompositeOperation = 'lighter'`** で描く。
描き終わったら必ず `ctx.save()` / `ctx.restore()` で閉じる。

| 対象 | 表現 |
|---|---|
| プレイヤー | スプライトの下に手の `glow` 色のラジアルグラデ（半径 `radius * 1.8`、中心 `55` → 外周 `00` アルファ） |
| 敵の手 | 同上だが `base` 色を使う（自機より一段くすませて味方/敵を差別化） |
| 弾 | 進行方向の逆へ伸びる残光ストリーク（長さ `radius * 5`）+ 白芯のオーブ（紫 `#e0b3ff` → 透明） |
| パーティクル | 加算合成 + `shadowBlur: 8`、寿命に応じてサイズとアルファが減衰 |
| アイテム（星） | 外周グロー（半径 `radius * 2.2`、`STAR_COLORS.glow` の `55` → `00`）+ 5稜星のパス（`core` → `base` の線形グラデ）。`timeSec * 1.2 rad/s` で回転、`1 + 0.08 * sin(timeSec * 4)` で脈動 |
| 無敵中の自機 | 手の `glow` 色はそのまま。外側に半径 `radius * 1.6` の黄色い残り時間リングを重ねる（`render/effects.ts`） |
| スキンプレビュー（ゲームオーバー） | 選択中スキンの `rock` スプライトを表示。未解放は `ctx.filter = 'brightness(0)'` で黒シルエット化し、直下に `UNLOCK <必要スコア>` を添える。新しい色は増やさない |

- スプライトの描画サイズは `radius * 2.4`（当たり判定より一回り大きく見せる）。
- スプライト読込失敗時は **手のキーカラー円 + 絵文字** にフォールバックする（`assets.ts`）。
  この互換性は維持する — 同名ファイルを置くだけで差し替え可能。
- スキン15枚は WebP（`tools/make-skins.py` で書き出し）。PNG のままだと計 1.04MB あり
  起動時の一括ロードに見合わない（WebP なら 215KB）。デフォルト自機と敵の 6 枚は PNG のまま。

---

## 7. モーション・演出

**短く、強く、すぐ収まる。** ゲームプレイを妨げる長い演出は入れない。

| 演出 | 時間 | 内容 |
|---|---|---|
| LVUP フラッシュ | 0.35s | 白 `alpha 0.6` から線形フェード |
| LVUP モーフ | 0.6s | プレイヤーが点滅（`Math.floor(morphSec * 12) % 2`）+ 「LEVEL UP!」バナーが上から降下 |
| GAME OVER シェイク | 0.4s | `translate` を毎フレーム ±7px ランダム |
| 点滅プロンプト | 周期 1.6Hz | `Math.floor(t * 1.6) % 2 === 0` で表示/非表示 |
| アイテム取得フラッシュ | 0.25s | イエロー `alpha 0.6` から線形フェード（LVUP の白と区別する） |
| 星の消滅予告 | 消滅前 2s | `Math.floor(残り寿命 * 8) % 2` で点滅 |
| 無敵リングの終了予告 | 残り 1.5s | `Math.floor(残り * 8) % 2` で点滅 |
| NEW RECORD | 常時 | `0.6 + 0.4 * sin(t * 6)` のアルファ脈動 |
| 背景グリッド | 常時 | 見下ろし 12px/s、遠近 0.25/s でスクロール |

- 点滅の位相は `performance.now() / 1000` を基準にする（シーン間で揃う）。
- GAME OVER のロゴは赤/シアンを ±13px ずらして `alpha 0.55` で重ね、グリッチ風の色ずれにする。
- シェイク中はリトライ入力を受け付けない（が、ラッチの排水は毎フレーム行う）。
- 無敵リングは LVUP のモーフ点滅の対象外。自機と一緒に消えると残り時間を見失うため。

---

## 8. 既知の逸脱（直すなら）

現状コードがこのルールから外れている箇所。新規コードでは真似しないこと。

- `scenes/play.ts` のパーティクル色が `'#f1c40f'` / `'#3498db'` とハードコードされている。
  本来は `COLORS.yellow` / 手の `glow` を使うべき箇所。
- `render/panel.ts` の Y 座標（48, 84, 140, …）はすべてマジックナンバー。
  項目の増減時は手で調整が必要。
- `scenes/title.ts` のルールカード位置・`FLOATERS` の座標も同様にハードコード。
  浮遊手はカード帯（y 420-570）と中央のプロンプトを避けて四隅寄りに置く、という制約がある。

---

## 9. 新しい要素を足すときのチェックリスト

- [ ] 色は `theme.ts` から取っているか（新色を足すなら `theme.ts` に追加）
- [ ] アクセント色の意味（シアン=進行 / 黄=良 / 赤=危険）と矛盾していないか
- [ ] フォントは `FONT_DISPLAY` / `FONT_NUM` のどちらかか
- [ ] 文字は `outlinedText` / `neonText` / 小ラベルのいずれかに収まっているか
- [ ] 発光は `'lighter'` + `save`/`restore` で閉じているか
- [ ] 数値HUDをフィールド内に置いていないか
- [ ] 描画関数は状態を持たず `(ctx, ...data)` を受けているか（アニメは時間引数で）
- [ ] 演出は1秒以内に収まっているか
